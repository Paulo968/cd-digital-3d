import React from 'react'
import ReactDOM from 'react-dom/client'
import ExperienceRoot from './app/ExperienceRoot'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ExperienceRoot />
  </React.StrictMode>,
)
